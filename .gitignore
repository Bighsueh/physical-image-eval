# Dependencies
node_modules/
.pnp
.pnp.js
.yarn/

# Build output
dist/
build/
out/
.next/
.vite/
*.tsbuildinfo

# Environment & secrets (NEVER commit)
.env
.env.*
!.env.example
*.pem
*.key
secrets/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Test / coverage
coverage/
.nyc_output/
test-results/
playwright-report/
blob-report/
.playwright/

# Docker / local data volumes
pgdata/
postgres-data/
*.local

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Image source is NOT committed (medical AIGC dataset, ~80M). For local testing it is bundled as a
# copy under images/ (mounted read-only by docker-compose); kept out of git history.
images/

# Generated 002 ingestion fixtures (built by test global-setup)
backend/tests/fixtures/source/

# Regenerable UI preview screenshots (design reference, not committed)
specs/design/preview/

# Local-only tooling (image text-repair pipeline etc.) — not part of the published app
tools/
